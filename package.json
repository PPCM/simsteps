{
  "name": "simsteps",
  "version": "0.1.0",
  "description": "Simulateur de flux d'entrepôt 3D — picking B2B/B2C",
  "type": "module",
  "license": "Apache-2.0",
  "scripts": {
    "sim": "node sim/cli.js",
    "dev": "node --env-file-if-exists=.env --watch server/index.js",
    "start": "node --env-file-if-exists=.env server/index.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@fastify/rate-limit": "^11.1.0",
    "@fastify/static": "^9.1.3",
    "fastify": "^5.9.0",
    "pg": "^8.22.0",
    "three": "^0.185.1"
  }
}
