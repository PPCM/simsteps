# Changelog - 2026-07-05

## Summary
**Corridor network**: corridors become first-class objects (list, minimum 1) with editable position, length, lane width and orientation (horizontal/vertical dropdown); crossing or touching corridors connect automatically, forming real circulation paths between zones. The graph builder rewires accordingly: coordinate-keyed station nodes at endpoints/intersections, aisle ends connect to the nearest crossing horizontal corridor (at least one outlet required, dead-end aisles allowed), facilities attach by projection onto the nearest corridor, and a BFS connectivity check rejects broken networks with precise French errors. The historical `{frontY, backY}` format stays accepted everywhere and is normalized to two full-width corridors when editing. The engine is untouched (A* on the graph).


Display option: a « Masquer les libellés » switch in the Piloter tab hides all 3D label sprites (aisles, zones, corridors); while hidden, clicking an element — corridor bands included — reveals its own label (click in the void hides it again).

Editor follow-up on floor resizing: changing the warehouse width/depth now recenters the camera on the whole new floor (the extra depth grows toward the viewer and was off-screen, making the new space unreachable by drag), with a floor-proportional framing that keeps every corner visible. The front/back corridors — which bound aisle movement and were invisible (reported as "objects can't move along the whole y axis") — are now drawn on the floor as translucent circulation bands.

## Changes

- [web/public/js/main.js] Changing the floor width/depth in the global fields recenters the camera (`applyWorkingDef` forwards a `recenter` flag); all other edits still keep the user's viewpoint; « Masquer les libellés » switch wired to the scene, with click-to-reveal on the canvas (orbit-end clicks filtered out by pointer travel) and `labelStats` added to the `window.simstepsDebug` test handle
- [web/public/js/scene.js] Recenter framing scales with the floor (camera at width/2 + span·0.5, span·1.05, depth/2 + span·0.95 with span = max dimension): the whole floor, near edge included, fits on screen after a resize — the previous framing cut the near edge for deep floors; front/back corridors rendered as translucent floor-marking grey bands (distinct from the receiving zone's steel blue), non-pickable, rebuilt with the statics so they follow the Couloir avant/arrière fields live, each labelled « Couloir avant » / « Couloir arrière » (labels placed right of the racks where nothing overlaps in the default view); label sprites tracked with their element (`setLabelsVisible`, `revealLabel`, `labelStats`, `pick`) so the global switch and the click-to-reveal survive scene rebuilds; corridor bands wrapped in `{type: 'corridor'}` groups, pickable like any element: click reveals their label, and in edit mode they are selectable (panel shows « Couloir avant/arrière » with a Position (y) field synced with the global fields) and draggable along y via `moveCorridor` (metre snap, clamped between the floor edge and the aisles' outlet margin); deleting a corridor is refused with a French message
- [web/public/js/layout.js] Added `corridorBands` (pure): front/back corridor strips spanning the full floor width, with French labels
- [tests/unit/web/layout.test.js] corridorBands positions, extent and labels
- [tests/ui/editeur.spec.js] New test: resizing the floor depth (computed from the disposable warehouse, not hardcoded) recenters the camera on the new floor centre while other edits leave it untouched (existing test)
- [web/public/index.html] « Masquer les libellés (clic : révéler) » switch in the Affichage section
- [web/public/js/editor/model.js] Added `moveCorridor` (pure): metre-snapped corridor move bounded by the floor edge (1 m), the aisles' outlet (CORRIDOR_MARGIN) and the other corridor
- [web/public/js/editor/panel.js] Corridor selection panel: French title and a single Position (y) field
- [tests/unit/web/editor-model.test.js] moveCorridor clamps/snap/non-mutation and validity of the moved definition
- [tests/ui/editeur.spec.js] New test: click-select the front corridor band (camera-projected point), drag it toward the floor edge — integer position, global field synced, no validation error
- [tests/ui/interface.spec.js] New test: hide labels, reveal one by clicking a rack (screen position computed by camera projection, independent of warehouse dimensions), hide it with a void click, reveal a corridor's label by clicking its band, restore all by unchecking

## Notes
Verified end-to-end in a real browser on a disposable copy: with depth 42 → 62, the far corners and the near edge now project inside the canvas, and a real pointer drag brings the Réception zone to the very edge of the floor (front edge y = 59, back edge = 62 = floor boundary). The default view of the example warehouse improves as a side effect (the Réception zone was previously cut off at the bottom). The remaining "can't move along the whole y axis" symptom was the back corridor doing its job invisibly: aisles must stay between the corridors, which are now visible on the floor. 164 unit tests and 13 UI tests pass.

Release v0.4.0 published (corridor network with connectable/orientable/resizable corridor objects, 3D-label hiding with click-to-reveal, camera recentre on floor resize, draggable corridors, floor-exact rectangular grid, axis-labelled global fields): full suite green beforehand (171 unit + 15 UI), tag pushed, Docker Hub multi-arch image published and chart auto-aligned by the workflow.

## Changes (corridor network)

- [sim/warehouse.js] Graph builder rewritten as a corridor network: `corridorList` exported (historical `{frontY, backY}` → two full-width corridors), coordinate-keyed station nodes shared at corridor crossings/touch points, aisle outlets to the nearest crossing horizontal corridor beyond each end (≥ 1 required, dead-ends allowed), facility attachment by projection onto the nearest corridor, per-corridor station chaining, BFS connectivity check with precise French errors («l'allée A3 ne débouche sur aucun couloir», «réseau de circulation non connexe : …»)
- [server/validate.js] `corridors` accepts the historical object or a non-empty list of `{id, x, y, length}` segments
- [web/public/js/layout.js] corridorBands renders the corridor list (horizontal/vertical segments, per-corridor lane width and label), historical format converted
- [web/public/js/scene.js] corridor labels placed at the band centre (generic for any segment)
- [web/public/js/editor/model.js] corridors normalized to a list on entering edit mode; `addCorridor`, `removeCorridor` (minimum 1 kept), `moveCorridor` rewritten (both axes, metre snap, segment clamped inside the floor), `updateCorridor` (id, label, x, y, length, width, orientation); aisle drag freed from corridor bounds (floor bounds only — connectivity is checked by validation); validateDefinition validates the corridor list (unique ids, positive sizes, valid orientation, footprint inside the floor) and drops the old «entre les couloirs» aisle rule; minimalDefinition in list format; updateGlobals loses frontY/backY
- [web/public/js/editor/panel.js] Corridor selection panel with all fields, orientation as a dropdown (select support added to renderFields); global fields reduced to name/width/depth
- [web/public/js/main.js] Corridor branches for field edits (updateCorridor), two-axis drag preview/commit, removal via removeCorridor; « + Couloir » button with auto-selection
- [web/public/index.html] « + Couloir » button in the edit window
- [tests/unit/sim/warehouse.test.js] Network tests: connected multi-corridor network with vertical link and dead-end aisle, aisle without outlet rejected, disconnected network rejected
- [tests/unit/web/editor-model.test.js] moveCorridor rewritten (snap, floor clamps, non-mutation), addCorridor/updateCorridor (orientation flip to a connecting vertical corridor)/removeCorridor (last one protected, single-corridor network with dead-ends stays valid), validation updates
- [tests/unit/web/layout.test.js] corridorBands: historical conversion and horizontal/vertical segments
- [tests/ui/editeur.spec.js] Corridor drag test rewritten: camera-projected drag target (deterministic at any zoom), edit window collapsed during the gesture (its grown panel covered the drag point and swallowed the pointer — found by instrumenting pointer events), reopened to exit
- [tests/ui/interface.spec.js] Front corridor position computed from either corridors format

## Notes (corridor network)
171 unit + 15 UI tests pass. Real-browser verification on a disposable copy: « + Couloir », orientation flipped to vertical via the new dropdown, positioned to link the two transversal corridors (no validation error), saved, and the simulation ran on the network (orders processed, operators routing through the vertical link). The Playwright corridor-drag failure took real debugging: pointer-event instrumentation revealed the edit window — grown by the 7-field corridor panel — covered the drag point, so the mouse was dragging the window's title bar, not the corridor.

## Changes (corridor network — phase 2)

- [web/public/js/editor/model.js] Endpoint magnetism on corridor drag (`magnetShift`, range 1.5 m): after the metre snap, if the moved corridor's nearest endpoint is within range of a perpendicular corridor's axis — and the junction would land on that corridor's span — the segment shifts along its own axis to close the gap (T-junction); already-crossing or touching segments are left alone; floor clamps still apply last
- [web/public/js/layout.js] Added `corridorJunctions` (pure): deduplicated connection points — horizontal × vertical crossings and coincident endpoints between any two corridors
- [web/public/js/scene.js] Junction discs rendered on the floor at every connection point (corridor-grey, brighter than the bands), rebuilt with the statics
- [tests/unit/web/editor-model.test.js] Magnetism: 1 m gap closed, 2 m gap out of range, touching/crossing untouched, junction required to land on the other corridor's span
- [tests/unit/web/layout.test.js] corridorJunctions: two crossings via a linking vertical corridor, corner via coincident endpoints, none for the historical parallel corridors

## Notes (corridor network — phase 2)
174 unit + 15 UI tests pass. Real-browser check on a disposable copy: a vertical corridor dragged to 1 m from the front corridor snaps onto it (y 5 → 4, no validation error), and the junction discs appear at the crossings of the corridor bands.

## Changes (network editing fixes)

- [web/public/js/editor/model.js] updateCorridor: an orientation flip now pivots the segment around its centre (it used to pivot around its start point, teleporting the far end), and any geometry change (orientation, length, width) re-clamps the segment inside the floor — a flip near an edge no longer produces an out-of-floor segment whose render silently lagged behind the model
- [web/public/js/main.js] The edit scene now follows the working definition even when the circulation network is invalid (island corridor, unreachable zone): the band lands where the user put it, the French error is listed and Enregistrer stays disabled; only geometric errors (unbuildable plan) freeze the scene on the last sane state — this removes the model/visual divergence that read as "the corridor keeps snapping back to its first magnetised position"; the debug handle also exposes the scene for UI tests
- [tests/unit/web/editor-model.test.js] Centre pivot (including round-trip), edge-flip containment, length change re-clamping
- [tests/ui/editeur.spec.js] New test: an invalid network state stays visible (band position read from the scene), reported («non connexe») and unsaveable, then clears when the corridor reconnects through an aisle outlet

## Notes (network editing fixes)
Both user-reported symptoms shared one root cause: applyWorkingDef only rebuilt the scene for fully valid definitions, so a model change that broke the network (orientation flip overflowing the floor, drag creating an island that captured the nearest zone) left the visuals on the last valid state — the flip "waited for the next move" and dragged corridors "kept coming back to the first magnet position". 176 unit + 16 UI tests pass.

Release v0.4.1 published (endpoint magnetism, visible junction discs, centre-pivot orientation flip, edit scene faithful to the model on invalid networks): full suite green beforehand (176 unit + 16 UI), tag pushed, Docker Hub multi-arch image published and chart auto-aligned by the workflow.

- [doc/roadmap-logistique.md] Created: logistics gap analysis (material handling equipment taxonomy with simulation implications, parametric racks, missing warehouse concepts — docks, buffer/drop zones feeding the packing workshops with a distinct packer role, staging, SKU/ABC slotting, inbound flows, congestion) and a six-phase integration plan (racks → fleet → slotting → inbound & process zones → circulation → automation)

## Changes (roadmap phase 1 — parametric racks)

- [web/public/js/layout.js] rackBoxes: per-rack `depth` (default 1.4 m) and `levelHeight` (default 2 m); height = levels × levelHeight; levels/levelHeight exposed for the level beams
- [sim/engine.js] `liftTimePerLevelSec` scenario parameter (default 6 s): picking a line costs pickTime + (level − 1) × lift; order lines carry their slot's level
- [server/validate.js] liftTimePerLevelSec bounds (0–3600)
- [web/public/js/editor/model.js] normalize makes rack depth/levelHeight explicit; updateAisle applies levels/levelHeight/rackDepth to both racks of the aisle; aisleHalfWidth derived from the aisle's deepest rack (drag clamps and footprint follow); addAisle copies the rack profile; updateGlobals accepts `dimensions.height` (optional ceiling); validation: levels integer ≥ 1, positive sizes, rack height ≤ ceiling when defined
- [web/public/js/editor/panel.js] Aisle panel gains Niveaux de rack / Hauteur de niveau / Profondeur racks (applied to both aisle racks); global field Hauteur plafond (z)
- [web/public/js/scene.js] Racks rendered at real heights with level beams (line frames at each intermediate level)
- [tests/unit/*] rackBoxes sizes and offsets, updateAisle rack props, ceiling validation, engine lift-time effect on cycle time (deterministic same-seed comparison)
- [tests/ui/editeur.spec.js] New test: set rack levels/level height from the aisle panel, check the 3D boxes reach 7.5 m, save and verify both racks persisted
- [README.md] Rack fields, `dimensions.height`, `liftTimePerLevelSec` in the scenario table, editor bullet
- [doc/roadmap-logistique.md] Phase 1 marked done (rack `type` deferred to the typology work)

## Notes (roadmap phase 1)
180 unit + 17 UI tests pass. Real-browser check on a disposable copy: aisles at 3 × 2.2 m and 5 × 2.2 m (deeper racks) tower over the single-level ones with visible level beams, slot count doubles accordingly (408), and the simulation runs with the lift-time cost.

## Changes (roadmap phase 2 — vehicle fleet)

- [sim/vehicles.js] Created: 7 vehicle profiles (walker, pallet trucks, stacker, counterbalance, reach truck, VNA, order picker) with loaded/unloaded speeds, lift height and minimum aisle gauge; `fleetFromScenario` (legacy `operators` = walkers)
- [sim/graph.js] Edges carry a lane width; `shortestPath({minWidth})` and `reachableFrom(source, minWidth)` filter by gauge; `distancesFrom` (Dijkstra) added for slotting
- [sim/warehouse.js] Aisle chains/outlets and corridor chains carry their lane widths; slots carry their rack's levelHeight; connectivity check reuses reachableFrom
- [sim/engine.js] Agents built from the fleet with vehicle profiles (walker speed follows scenario speedMps), gauge-filtered routing, loaded speed after the first pick, mission compatibility (lift height + reachability per gauge), fleet-impossible missions dropped with lines marked `unreachable`
- [server/validate.js] `fleet` parameter validated (known types, integers 0–500, total ≥ 1)
- [web/public/js/projects.js] `fleet` joins the panel-driven keys
- [web/public/js/operators.js] Per-family 3D silhouettes (boxes at vehicle gauge, walker keeps the capsule), same state colours
- [web/public/js/main.js] Fleet counters generated from the catalog in the Piloter tab (tooltips with gauge and lift), wired into settings/projects/status (agent total), empty-fleet guard, vehicle map passed to the operator layer
- [web/public/index.html] « Engins de manutention » block in the Scénario section
- [tests/*] Graph gauge filtering, fleet validation, engine accessibility (walkers vs high levels, forklift stuck in narrow aisles, VNA fit), mixed-fleet determinism and legacy equivalence, projects fleet split, UI fleet counter test; the window-drag UI test collapses the (now taller) main window first

## Changes (roadmap phase 3 — SKUs & slotting)

- [sim/skus.js] Created: one reference per slot, ABC rotation classes (20/30/50 % of slots drawing 80/15/5 % of lines), `buildSlotting` ranks slots randomly (Fisher-Yates on the scenario rng) or by Dijkstra distance from shipping (`abc`), returns a deterministic rotation-weighted `drawSlot`
- [sim/orders.js] makeOrder accepts an injected `drawSlot` (uniform fallback preserved)
- [sim/engine.js] `slotting` scenario parameter (default `aleatoire`) wired into order generation
- [sim/kpi.js] `distancePerLineM` KPI (+ console row) — the slotting indicator
- [web/public/js/kpiSampler.js] distancePerLineM in live samples
- [web/public/js/compare.js] « Distance / ligne » comparison row (less is better)
- [server/validate.js] `slotting` values validated
- [web/public/js/projects.js] `slotting` joins the panel-driven keys
- [web/public/index.html + main.js] « Rangement (slotting) » selector in the Scénario section, « Distance / ligne » row in the live KPIs
- [tests/*] skus unit tests (class shares, 80 % concentration on A, ABC proximity ordering, determinism), engine test « ABC beats random on distance per line » (same seed), compare fixture updated, UI test extended (slotting switch + live KPI fed)

## Notes (roadmap phases 2–3)
191 unit + 18 UI tests pass. Real-browser check: fleet counters and slotting selector live in the Piloter tab, two pallet trucks idle at shipping on the historical warehouse (their 1.9 m gauge exceeds the 1.4 m corridors — expected and visible), walkers keep picking, « Distance / ligne » feeds live (29,7 m). The engine test shows ABC slotting cutting distance per line versus random placement at identical demand.

## Changes (agent parkings)

- [sim/warehouse.js] Optional `parkings` zone list, attached to the network like any facility and exposed with node ids
- [sim/engine.js] Each agent is assigned the nearest gauge-reachable parking (fallback: shipping) as `startNodeId`, starts there and travels back when idle after a mission (empty-run trip counted in distance, outside busy time); returning agents become assignable on arrival
- [web/public/js/timeline.js] `finish(agents)` takes per-agent start nodes and creates tracks for agents that never moved
- [web/public/js/editor/model.js] addParking / removeParking (optional, zero allowed), parkings in facility lookup, uniqueness and footprint validation, normalization
- [web/public/js/{layout,scene,editor/panel,main}.js + index.html] « + Parking » button, violet zone patches, selection panel, drag/resize like any zone
- [server/validate.js] optional `parkings` list validated
- [tests/*] engine start/return-to-park and legacy fallback, editor parking ops, UI test (add + save a parking), timeline signature updates

## Changes (operator/vehicle decoupling + parking compatibility)

- [sim/engine.js] Walkers are the human operator pool; other fleet types are parked equipment. Foot-compatible missions (walker lift/gauge, precomputed per mission) go straight to the nearest idle operator; vehicle-requiring missions couple a free operator with a free compatible vehicle: the operator walks to the vehicle (unloaded walking speed), mounts (new human state `driving`), the vehicle entity runs the mission, chains further vehicle-requiring missions without dismounting, returns to its own parking, then the operator walks back to his call point. Driver busy from walk start to return; vehicle busy per mission. A fleet without walkers moves nothing; missions impossible for the whole fleet stay marked unreachable
- [sim/warehouse.js] Parkings expose their optional `vehicles` list (admitted types)
- [sim/engine.js] Parking assignment filters by admitted types in addition to gauge reachability
- [web/public/js/operators.js] Human capsule hidden while in state `driving` (inside the cab)
- [web/public/js/editor/{model,panel}.js] Parking panel gains « Engins admis » (CSV ↔ list, empty = all), validated against known vehicle types (local list synced with sim/vehicles.js); updateFacility can clear the restriction
- [server/validate.js] parkings entries may carry a `vehicles` list of known types
- [tests/unit/sim/engine.test.js] Mount/drive/return episode test (parking restricted to the vehicle type: machine starts there, human does not; `driving` state observed; driver busy time > 0), machine-only fleet moves nothing, gauge/lift tests updated with drivers

## Changes (parking admitted-vehicles UX)

- [web/public/js/editor/panel.js] « Engins admis » switched from free-text CSV to a checkbox group built from the vehicle catalog (French labels; none checked = all admitted); new `checkboxes` field type in renderFields
- [web/public/index.html] check-grid/check-item styles; the checkbox field spans the full selection grid (`.field-wide`) so long labels no longer overflow onto the global properties column
- [web/public/js/editor/model.js] Removed the now-unused CSV ↔ list conversions in displayValue/modelValue (the panel sends the list directly)
- [tests/ui/editeur.spec.js] Parking test now checks two vehicle types via the checkboxes and verifies the persisted list

## Release
v0.5.0 published: roadmap phases 1–3 (parametric racks, vehicle fleet with operator/vehicle decoupling, ABC slotting), agent parkings with admitted vehicle types. Multi-arch image pushed to Docker Hub (0.5.0, 0.5, latest); Helm chart aligned by CI.

## Changes (roadmap phase 4 — inbound flows and process zones)

- [sim/engine.js] Behind `replenishment` (off by default): finite stock per picking slot (level 1) drawn down at order creation, reserve pallets on levels ≥ 2 (same-column reference), threshold-triggered priority replenish missions (vehicle-only, vertical move at the same node), inbound trucks (`inboundTrucksPerDay`, exponential arrivals) delivering pallets put away to free reserve slots (saturated reserve = pallets waiting, re-placed when a replenishment frees a slot), mission `kind` field with per-kind pick/drop durations and effects, order planning no longer blocked by waiting flux missions, KPI counters (replenishments, putaways, stockouts, palletsWaiting). Packer role: with `packers` ≥ 1 and buffer zones, B2C lines are staged at the nearest buffer and a packer carries them to the workshop and packs (packTimePerOrderSec per distinct order) before orders complete
- [sim/orders.js] Slot draws bounded (stock-starved pools no longer loop); null draws skipped
- [sim/skus.js] buildSlotting accepts a slot subset (picking slots when replenishment is on)
- [sim/warehouse.js] Optional `buffers` zones attached to the network and exposed
- [server/validate.js] `replenishment` boolean, flux numeric bounds, `buffers` list validated
- [web/public/js/editor/model.js] addBuffer/removeBuffer (optional, zero allowed), buffer in lookups/normalization/validation/edge-coordinate conversions
- [web/public/js/{layout,scene,editor/panel,main}.js + index.html] « + Tampon » button (olive zones), Flux block in the Piloter tab (replenishment switch, trucks/day, packers) wired into settings and projects
- [web/public/js/projects.js] replenishment/inboundTrucksPerDay/packers join the panel-driven keys
- [tests/*] Engine flux tests (replenishment feeds picking, walker-only fleet starves, putaway after reserve frees, determinism, legacy KPIs unchanged, packing decoupling lengthens cycles), validation and editor buffer tests, UI test (add + save a buffer)

## Notes (roadmap phase 4)
203 unit + 20 UI tests pass. Real-browser check on a disposable 3-level copy with wide lanes: Flux block drives the sim (24 trucks/day, 2 packers, replenishment on), 612 slots (204 picking + 408 reserve), buffer and parking zones rendered, orders complete with packing included in the cycle time. Simplifications vs the plan: docks = existing receiving zones (no door scheduling), staging capacity unbounded — noted in the roadmap.

## Changes (roadmap phase 5 — advanced circulation)

- [sim/graph.js] Edges carry an access class ('mixte'/'pietons'/'engins'); shortestPath/reachableFrom filter by gauge AND agent class; oneWay edges already supported
- [sim/warehouse.js] Corridors accept `oneWay` ('positif' toward +x/+y, 'negatif' toward −x/−y — directed chaining, invalid values rejected in French) and `access`; connectivity check upgraded to STRONG connectivity (aisles/zones you cannot return from → explicit error; corridor endpoints tolerated as dead ends)
- [sim/engine.js] Reach sets keyed by (gauge, class); routing passes the agent class. Aisle exclusivity: a vehicle wider than half the aisle lane locks the aisle it crosses — routes are split into legs at lockable-aisle boundaries, other agents (walkers included) wait in FIFO queues at the ends (new `waiting` state), the lock releases when the next leg leaves the aisle and wakes the queue (leading walkers pass, the first locker recloses); waiting agents never hold a lock (no deadlock); walkers never lock (all-walker runs unchanged). Per-agent waitTime + `waitingTimeSec` KPI. Fixed a latent single-node-path hole where an agent stayed idle and could be double-assigned
- [sim/kpi.js] Console row « Attente aux allées (congestion) » when non-zero
- [web/public/js/operators.js] `waiting` state colour (red); [web/public/index.html] legend chip « Attente »
- [web/public/js/editor/{model,panel}.js] Corridor panel gains « Sens unique » and « Accès » selects (« non »/« mixte » keep the fields absent); obstacles: addObstacle/removeObstacle (1×1×3 m default, first against the west edge), normalization, footprint-in-floor and overlap validation against aisles (racks included), zones and corridors
- [web/public/js/{layout,scene,main}.js + index.html] obstacleBoxes geometry, solid grey blocks with labels (pickable, draggable, resizable), « + Obstacle » button
- [server/validate.js] optional `obstacles` list validated
- [tests/*] Graph class filters, one-way corridor orientation + strong-connectivity rejection + invalid values, engine reserved-lane blocking, congestion (waiting state observed, waitingTimeSec consistency, walkers-only = zero waits, determinism), editor obstacle ops + overlap detection, layout obstacleBoxes, UI test (add + save an obstacle)

## Notes (roadmap phase 5)
213 unit + 21 UI tests pass. Real-browser check on a disposable copy (3-level racks, 1.7 m aisles, 2 VNAs): « Attente » legend live, obstacle rendered as a solid block. Simplifications vs the plan (noted in the roadmap): per-edge capacity is represented by the aisle-exclusivity rule only, and walkers inside an aisle do not block an incoming locking vehicle.

## Changes (flux demo data)

- [data/warehouse-flux.json] Created: 60×46 m demo warehouse for phases 1–5 — 3 wide pallet-rack aisles (2.8 m, 3 levels × 2.2 m) + 4 VNA aisles (1.7 m, 4 levels), 800 slots, west link + east vehicles-only corridor, packing buffer, dedicated parkings (machines / walker call point), dock receiving zone, two columns (obstacles), 11 m clear height
- [data/scenario-flux.json] Created: « Flux complet — flotte mixte » — 5 walkers + 2 reach trucks + 2 VNA, ABC slotting, replenishment (45 u/slot), 24 inbound trucks/day, 2 packers, 25 orders/h (aligned on the slider step)
- [db/seed.js] First-start seed now inserts the flux warehouse and the flux scenario alongside the historical example
- [tests/unit/server/validate.test.js] Guard test: the shipped demo files pass server validation
- Running database: example warehouse (id 1) restored to its original definition; flux warehouse and scenario inserted via the API

## Notes (flux demo)
Balanced so the demo shows healthy pressure: 51/64 orders completed over 2 h, 56 % occupancy, visible aisle-lock waiting, 103 replenishments, 32 putaways, both machine types and both packers active, zero stockouts.

## Changes (full-fleet demo rework)

- [sim/engine.js] Machine dispatch: among idle compatible vehicles, the LEAST-USED one is chosen (deterministic load rotation) instead of the first in fleet order — mixed fleets now spread work across every type; all-walker runs unaffected
- [data/warehouse-flux.json] Reworked so every vehicle type has a niche: A1 and A2 widened to 3.6 m (counterbalance truck territory, aisles shifted for footprints), east block behind vehicles-only corridors (C5/C6 horizontal + C4) with ground-level aisle A8 (2.4 m, 1 level) — walkers cannot reach it, so its picking waves go to pallet trucks/stackers; 832 slots
- [data/scenario-flux.json] All 7 vehicle types in the fleet (5 walkers, 1 of each machine, 2 VNA), zoneWave strategy (waveSize 12) so east-block waves are machine-pure, random ABC placement (spreads demand onto the east block), tighter replenishment trigger (24 u/slot, 60 % threshold), 48 trucks/day
- Running database updated (warehouse + scenario PUT)

## Notes (full-fleet demo)
User feedback: machines were invisible in the demo (first mission after ~5 sim-minutes, only 2 types). Now every type starts within the first ~18 sim-minutes (pallet truck 2 min, stacker 3, VNA 3, counterbalance 6, reach truck 11, order picker 18) with 9–52 % occupancy each; packers active from minute 2. Incomplete orders at the horizon are all late-created (in flight), no structural starvation. 214 unit + 21 UI tests pass.

## Changes (style-5 vehicle rendering)

- [web/public/js/operators.js] Machines rendered as low-poly models (style 5 of the mockup board): per-type part lists (`VEHICLE_MODELS`, exported and Node-testable) composed from a shared unit-box geometry and shared fixed materials — orange body, steel mast, grey forks, dark wheels/guards; driver capsule visible only while on mission (standing behind the tiller for pallet truck/stacker); state carried by a ground ring (y 0.07, above corridor bands and heatmap); vehicles rotate to face their direction of travel (front = local +x); idle vehicles glide onto distinct parking spots (damped 4-per-row grid) instead of interpenetrating on the parking node; walkers/packers keep the state-colored capsule
- [tests/unit/web/operators.test.js] Created: every catalog machine type has a model, exactly one driver, wheels, forks, positive above-ground parts
- [tests/ui/interface.spec.js] Fleet test asserts the scene contains the low-poly groups (userData.vehicleType)
- [README.md] Vehicle rendering description and state-color legend (capsule vs ground ring)

## Notes (style-5 rendering)
User-selected style 5 from the mockup board (no beacon, walk-behind operators). Verified in-browser on the flux demo: pallet truck crossing the east block with its walking operator behind and correct heading, parked fleet spread on distinct spots with grey idle rings. 217 unit + 21 UI tests pass.

## Changes (mounted-driver visibility fix)

- [web/public/js/operators.js] A walker's capsule is hidden while its state is `driving` (the operator is at the wheel — the machine's driver capsule already appears): it no longer lingers greyed-out at the parking during the whole drive
- [tests/unit/web/operators.test.js] Regression test with a synthetic walk → drive → walk track asserting capsule visibility

## Changes (VNA driver visibility)

- [web/public/js/operators.js] The VNA cab box was swallowing its driver capsule (0.23 m visible): cab lowered, driver raised, roof lifted — 0.55 m of torso now shows
- [tests/unit/web/operators.test.js] Guard test: for every model, the driver capsule must poke ≥ 0.35 m above any solid box overlapping its footprint (thin roofs/rails excluded)

## Release
v0.6.0 published: roadmap phases 4–5 (finite stock & replenishment, inbound trucks & putaway, buffer zones & packers, one-way and reserved lanes, aisle-exclusivity congestion with waiting KPI, obstacles), full-fleet demo warehouse/scenario, low-poly oriented vehicle models with visible drivers and ground state rings. Multi-arch image pushed to Docker Hub (0.6.0, 0.6, latest); Helm chart aligned by CI.

## Changes (roadmap phase 6 — automation)

- [sim/vehicles.js] Two automated types: `agv` (pallet AGV) and `amr` (mobile robot), flagged `automated` — driverless
- [sim/engine.js] Automated vehicles start missions directly (no operator/vehicle coupling); battery per automated agent (mission-time discharge over `agvAutonomyHours`, default 4 h), 20 % threshold → return to their parking (charging station) and recharge 3× faster (new `charging` state, unavailable meanwhile), `chargingTimeSec` KPI; packer entities carry the battery fields too (NaN fix). Conveyors: staged B2C drops at a conveyor-fed buffer enter at the belt's `throughputPerMin` then travel at 0.5 m/s before creating the packing job at the attached workshop (packers stop walking); `conveyed` KPI when the warehouse has conveyors
- [sim/warehouse.js] `conveyors` parsed and attached to the nearest buffer (source) and workshop (sink); French errors when either is missing
- [web/public/js/operators.js] Low silhouettes with sensor for AGV/AMR (no driver capsule), violet `charging` ring state
- [web/public/js/{layout,scene}.js] conveyorBelts geometry, dark deck between light rails, labeled and pickable
- [web/public/js/editor/{model,panel}.js + main.js + index.html] « + Convoyeur » button, drag, panel (position, length, orientation, throughput), floor-footprint validation, normalization; agv/amr in the parking vehicle checkboxes; « Charge » legend chip
- [server/validate.js] `agvAutonomyHours` bounds; fleet accepts the new types (catalog-driven)
- [data/warehouse-flux.json + scenario-flux.json] Demo: packing conveyor between the buffer and workshop 1, +1 AGV +1 AMR (6-min autonomy so recharges are visible), parking admits the automated types; running DB updated
- [doc/roadmap-logistique.md] Phase 6 marked done; goods-to-person (needs a mobile-rack storage model) and mezzanines (structural only) deferred with rationale
- [tests/*] Automated-mission (no driver, walkers untouched), charge cycles (state observed, KPI consistency), legacy zero charging, conveyor transport + throttled-throughput bottleneck + missing buffer/workshop rejection, editor conveyor ops, layout belts, UI test (add buffer + conveyor, save)

## Notes (roadmap phase 6)
226 unit + 22 UI tests pass. Real-browser check on the flux demo: AGV/AMR in the fleet counters and scene, « Charge » legend, conveyor rendered between buffer and workshop, 16/17 orders completed at the 45-minute mark. All six roadmap phases are now shipped.

## Changes (two demo projects, cleaned database)

- [sim/engine.js] Fully-automated fleets now work: the assignment loop also lives on idle automated vehicles (no walker required), foot-compatible missions fall through to machines when no human is available, manned-vehicle coupling is guarded by human availability, and the fleet-impossibility rule distinguishes automated from manned compatibility
- [data/warehouse-amr.json + scenario-amr.json] Created: « Entrepôt robots SimSteps » (36×30 m, 144 slots, 1.3 m aisles, vehicles-only corridors, charging station, buffer + packing conveyor) and « Robots mobiles (AMR) » (6 AMRs, 2 packers, 9-minute autonomy so charge cycles show, ABC slotting) — balanced: 96/98 orders, 49 min cumulated charging, 90 conveyed jobs
- [db/seed.js] First-start seed now inserts the two demo pairs and their projects « Flux complet » and « Robots mobiles » (the historical example warehouse is no longer seeded; data/project-example.json removed)
- [tests/ui/helpers.js] createTestWarehouse posts the historical fixture read from data/warehouse-example.json (editor tests keep a known geometry whatever the database holds)
- [tests/ui/interface.spec.js] Fleet test computes the expected agent total from the panel (base scenario may already include machines)
- Running database: AMR warehouse and scenario created, projects « Flux complet » and « Robots mobiles » created, historical warehouse (example) and scenarios (base, waves) deleted

## Changes (walkers demo project)

- [data/warehouse-pietons.json + scenario-pietons.json] Created: « Entrepôt piétons SimSteps » (the proven historical 44×42 geometry, 204 slots, plus a walker call-point parking) and « Préparation à pied » (8 walkers, zoneWave strategy, ABC slotting) — balanced: 86/88 orders, 45 % occupancy, zero aisle waiting
- [db/seed.js] Seed now inserts the three demo pairs and projects: « Piétons », « Flux complet », « Robots mobiles »
- Running database: walkers warehouse, scenario and project created
