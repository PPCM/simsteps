# Changelog - 2026-07-04

## Summary
All 7 steps of the delivery plan: standalone discrete-event simulation engine with unit tests and console KPI export; REST API + PostgreSQL (migrations, seed, CRUD, run recording) with a working docker-compose stack; static 3D render of the warehouse (Three.js, orbit camera); operator animation driven by the engine (in-browser simulation, recorded timeline, interpolated playback at x1/x10/x60); full UI panel with live KPIs, spaghetti trails, floor heatmap, run saving and comparison mode; Helm chart validated with lint and template; French README and end-to-end Docker verification from a clean volume.

Post-delivery feature: **projects** (a project groups a live warehouse + scenario + parameter overrides, no versioning) with optional run attachment and project-filtered comparison, plus a **3D warehouse editor** (click-select and grid-snapped drag of aisles/workshops/zones in the Three.js scene, property panels, add/remove aisles and workshops, full warehouse CRUD from the UI). UX rework: the single overloaded sidebar becomes two floating windows — pilot/configure tabs on the left, live indicators + comparison on the right — draggable by their title bar and collapsible to a live title bar, with state remembered across sessions.

## Changes

- [package.json] Created: package `simsteps` (version 0.2.0 after the projects/editor release), ES modules, license Apache-2.0 (aligned with the repository LICENSE), engines node >=22, scripts `npm test` (Node native runner), `npm run sim` (CLI), `npm run dev`/`start` (server); dependencies fastify, @fastify/static, @fastify/rate-limit, pg, three
- [sim/rng.js] Created: seeded deterministic PRNG (mulberry32) with randInt / randExponential / randChoice helpers
- [sim/eventQueue.js] Created: stable binary min-heap event queue ordered by (time, insertion order)
- [sim/graph.js] Created: circulation graph (positioned nodes, directed/bidirectional edges) with A* shortest path
- [sim/warehouse.js] Created: warehouse JSON loader building the circulation graph (bay nodes per aisle, front/back corridors, facilities) and the picking-slot map; shipping/receiving accept a single object (historical format) or a list (`facilityList` exported), `shippings`/`receivings` exposed with first-zone `shippingNodeId`/`receivingNodeId` kept for compatibility
- [sim/orders.js] Created: B2C (1-3 lines, qty 1-3) and B2B (10-50 lines, qty 5-20, client pool) order generation
- [sim/strategies.js] Created: pluggable picking strategies — orderByOrder (one mission per order, FIFO) and zoneWave (zone-grouped waves capped at waveSize)
- [sim/engine.js] Created: discrete-event engine — Poisson order arrivals, nearest-idle-operator assignment, serpentine pick routes, B2C drops at nearest workshop / B2B at shipping, busy-time and distance tracking, per-edge traffic aggregation exposed as `traffic` in the result; rendering hooks onEvent, onTravel (full path + duration at each departure) and onState (moving/picking/dropping/idle transitions); B2B lines drop at the nearest shipping zone (multiple zones supported)
- [sim/kpi.js] Created: KPI computation (orders/h, lines/h, avg distance, occupancy, cycle time, pending) and French console formatting
- [sim/cli.js] Created: runs a scenario JSON against a warehouse JSON and prints KPIs
- [data/warehouse-example.json] Created: example warehouse — 6 aisles, 12 racks, 204 picking slots, 2 workshops, shipping and receiving zones
- [data/scenario-example.json] Created: baseline scenario (orderByOrder, 5 operators, 30 orders/h, 70% B2C)
- [data/scenario-waves.json] Created: comparison scenario using the zoneWave strategy
- [tests/unit/sim/*.test.js] Created: tests covering rng, eventQueue, graph/A*, warehouse construction and reachability, order generation, strategies, engine end-to-end (determinism, KPI coherence, traffic invariant) and KPI computation/formatting
- [db/migrations/001_init.sql] Created: warehouses, scenarios, runs tables (JSONB documents, scenario snapshot + KPIs + aggregated traffic per run)
- [db/migrate.js] Created: versioned SQL migration runner, transactional, applied automatically at startup
- [db/seed.js] Created: inserts example warehouse and the two example scenarios on first start only
- [server/db.js] Created: pg pool creation and wait-for-database retry loop
- [server/validate.js] Created: pure validation of warehouse definitions (including full topological build; shipping/receiving accept a single zone or a non-empty list) and scenario parameters (bounds, known strategy, unknown keys rejected)
- [server/schemas.js] Created: shared Fastify schemas (route id coercion, run list filters)
- [server/routes/warehouses.js] Created: CRUD + JSON import/export for warehouses
- [server/routes/scenarios.js] Created: CRUD + JSON import/export for scenarios
- [server/routes/runs.js] Created: POST executes a simulation and persists it, GET list with filters, GET detail with traffic, DELETE
- [server/index.js] Created: bootstrap (wait DB → migrations → seed → listen) with graceful shutdown
- [web/public/index.html] Created: full-screen 3D page with left side panel — SimSteps header, transport (play/pause, x1/x10/x60, simulated clock, playback progress bar), scenario select, sliders (operators, B2C mix, order rate), spaghetti/heatmap switches, live KPI dashboard, save-run button, comparison section; import map for Three.js, inline SVG favicon, operator-state legend
- [web/public/js/layout.js] Created: pure geometry module (floor size, rack boxes, zone patches, aisle labels, slot count) computed from a warehouse definition, testable under Node; rack offsets derived from the aisle corridor `width` (default 1.4 m), zone patches sized per facility (`width`/`depth`, default 4.8 × 3), shipping/receiving lists supported, and `gridSegments` (metre grid covering exactly the rectangular floor, boundary lines added for fractional dimensions)
- [web/public/js/scene.js] Created: Three.js scene builder — floor + metric grid, rack volumes with edges and shadows, colored zone patches, text sprites, hemisphere + directional lighting, OrbitControls with damping
- [web/public/js/labels.js] Created: canvas-texture text sprites for 3D labels
- [web/public/js/main.js] Created: full orchestration — loads warehouse + scenarios from the API, runs the simulation in the browser (recorder + KPI sampler), rebuilds 3D layers on every parameter change, drives the playback clock (play/pause, x1/x10/x60, restart at end), refreshes live KPIs at 4 Hz, saves runs to the DB, comparison across current settings / scenarios / saved runs
- [web/public/js/timeline.js] Created: pure trajectory recorder plugged into engine hooks (onTravel/onState) and playback functions positionAt (constant-speed interpolation along paths) and stateAt
- [web/public/js/operators.js] Created: operator capsules colored by state (moving/picking/dropping/idle) with emissive glow, per-frame update from the timeline, dispose()
- [web/public/js/kpiSampler.js] Created: pure live-KPI computation (in-progress missions counted in occupancy), periodic sampling via the onEvent hook, kpiAt playback lookup
- [web/public/js/compare.js] Created: pure comparison-table builder — 7 KPI rows, French formatting, signed deltas oriented by indicator (more/less/neutral)
- [web/public/js/spaghetti.js] Created: per-operator 3D trails (fixed color order), revealed progressively up to the playback time via setDrawRange
- [web/public/js/heatmap.js] Created: floor bands per circulation edge colored by run traffic (sqrt scale, amber ramp), toggleable
- [server/app.js] Created: Fastify app factory with injected pool, /health probe, global per-IP rate limiting (@fastify/rate-limit, 300 req/min default, configurable — fixes the 15 CodeQL js/missing-rate-limiting alerts), static frontend serving, /vendor/three static route (Three.js served from node_modules, no build step), /sim static route (engine served to the browser)
- [tests/unit/web/layout.test.js] Created: geometry module tests (containment in floor, aisle not covered by racks, zone kinds and labels, slot count)
- [tests/unit/web/timeline.test.js] Created: recorder/playback tests (start position, constant-speed interpolation with turns, stationary after arrival, state transitions, zero-duration travel, successive segments)
- [tests/unit/web/kpiSampler.test.js] Created: live-KPI computation (busy mission included in occupancy), sampling cadence, final sample, kpiAt lookup
- [tests/unit/web/compare.test.js] Created: comparison rows (equality, oriented improvements, neutral distance, null cycle time, French formatting)
- [Dockerfile] Created: multi-stage build, node:24-alpine final image, non-root user
- [docker-compose.yml] Created: app + PostgreSQL 16 with healthcheck, persistent volume, db port exposed for dev mode
- [.env.example] Created: PORT and DATABASE_URL
- [.dockerignore] Created
- [.gitignore] Added node_modules/, .env and local tooling folders
- [tests/unit/db/migrate.test.js] Created: pending-migration selection tests
- [db/migrate-cli.js] Created: standalone migration runner (waits for DB, applies pending migrations, exits) — used by the Helm initContainer
- [LICENSE] Apache 2.0, inherited from the initial commit of the pre-existing GitHub repository (local history rebased onto it at first push)
- [helm/simsteps/Chart.yaml] Created: chart v2 with optional Bitnami PostgreSQL dependency (OCI, condition postgresql.enabled); version/appVersion now follow releases automatically (0.1.2)
- [helm/simsteps/values.yaml] Created: documented values — image (default ppcm/simsteps from Docker Hub), replicas, resources, service, disableable ingress, migrations toggle, database modes (sub-chart / externalUrl / existingSecret)
- [helm/simsteps/templates/_helpers.tpl] Created: naming/labels helpers, secretName and databaseUrl (required-error when no database mode configured)
- [helm/simsteps/templates/deployment.yaml] Created: app Deployment with /health liveness+readiness probes, DATABASE_URL from Secret, migration initContainer (same image, node db/migrate-cli.js)
- [helm/simsteps/templates/secret.yaml] Created: Secret with DATABASE_URL, skipped when existingSecret is provided
- [helm/simsteps/templates/service.yaml] Created: ClusterIP service on named http port
- [helm/simsteps/templates/ingress.yaml] Created: optional Ingress (className, annotations, TLS)
- [helm/simsteps/templates/NOTES.txt] Created: post-install notes in French (port-forward or ingress URL)
- [helm/simsteps/.helmignore] Created
- [.gitignore] Added helm/simsteps/charts/ (vendored dependency archives)
- [.github/workflows/docker-publish.yml] Created: Docker Hub publication workflow on version tags (v*.*.*) — multi-arch build (amd64/arm64), semver tags + latest, GHA build cache, manual dispatch producing an `edge` tag; after a successful publish, an `align-chart` job commits the released version into Chart.yaml/values.yaml/README on main (idempotent); actions updated to current majors (checkout v7, setup-qemu/setup-buildx/login v4, metadata v6, build-push v7) to clear Node 20 deprecation warnings
- [.github/workflows/codeql.yml] Created: CodeQL analysis workflow (JavaScript/TypeScript, security-and-quality queries) on push/PR to main and weekly schedule; actions updated to current majors (checkout v7, codeql-action v4) to clear Node 20 deprecation warnings
- [README.md] Created: French README — quick start (Docker), dev mode, Helm/Kubernetes deployment with values table, UI guide, REST API table, warehouse JSON format documentation, scenario parameters, how to add a picking strategy (with example), main tables schema, project structure, 3 evolution ideas
- [tests/unit/server/validate.test.js] Created: warehouse and scenario validation tests
- [tests/unit/server/app.test.js] Created: Fastify app tests via inject() with a fake pool — /health ok/503, 429 beyond the rate limit, default ceiling transparent for normal usage
- [db/migrations/002_projects.sql] Created: projects table (name, warehouse_id FK CASCADE, scenario_id FK SET NULL, settings JSONB) + nullable runs.project_id (FK SET NULL) with indexes
- [data/project-example.json] Created: example project (8 operators, 90% B2C, 60 orders/h, zoneWave strategy)
- [db/seed.js] Updated: captures inserted ids (RETURNING) and seeds the example project referencing the example warehouse and first scenario
- [server/validate.js] Added validateProjectPayload (name, warehouseId, optional scenarioId, settings validated as scenario params with the `name` key rejected)
- [server/routes/projects.js] Created: CRUD /api/projects mirroring scenarios.js, with 404 on missing referenced warehouse/scenario (runs.js convention)
- [server/routes/runs.js] Updated: optional projectId on POST (404 before simulation if unknown, persisted as project_id) and projectId filter + column on GET list
- [server/schemas.js] Added projectId to run list query schema
- [server/app.js] Registered project routes
- [web/public/js/scene.js] Refactored: definition-dependent statics (sun, floor, grid, racks, zones, labels) grouped in a rebuildable THREE.Group; the square GridHelper is replaced by a rectangular LineSegments grid built from layout's gridSegments — resizing one warehouse dimension no longer makes the grid overflow the floor on the other axis (which read as both dimensions changing and as phantom free space) with per-business-element sub-groups carrying userData; new setDefinition() (full resource disposal + camera recenter, skippable via `{recenter: false}` so 3D-editor changes keep the user's viewpoint) and getPickables() for the editor
- [web/public/js/projects.js] Created: pure client-side project logic (splitSettings/buildSettings/mergeProjectParams, slider keys vs extra overrides)
- [web/public/js/editor/model.js] Created: pure editor model — edge-aligned grid snap (`snapEdge`: a dragged zone's left/front edge and an aisle's left rack flank land on a metre line so integer-sized elements fill whole floor cells; aisle bay start snaps to whole metres in y; corridor/floor clamps take precedence at bounds; added zones/workshops are edge-aligned from the start), panel coordinate conversion (`displayValue`/`modelValue`: zone x/y fields show the left/front edge, integer after snapping, while the model keeps centres; millimetre rounding kills float noise), corridor/floor clamped moves (bounds derived from per-element sizes), add/remove aisle (with racks), workshop and shipping/receiving zones (at least one zone of each type enforced), `normalizeDefinition` (zones to lists + explicit default dimensions, applied on entering edit mode), resizable elements (aisle corridor `width`, zone `width`/`depth`), property updates with aisle-id propagation to racks, client-side validateDefinition (bays ≥ 2 guarding the NaN pitch case, id uniqueness, per-element footprint inside the floor, positive sizes, injected buildWarehouse), duplicateDefinition, minimalDefinition (list format)
- [web/public/js/editor/controls.js] Created: editor interactions — capture-phase raycast selection coexisting with OrbitControls, floor-plane drag with model-identical snap/clamp preview (no rebuild during drag), material-clone highlight, move cursor on hover
- [web/public/js/editor/panel.js] Created: DOM rendering of selection/global property fields and validation errors; width/depth fields on facilities, corridor-width field on aisles, zone lookup in shipping/receiving lists; facility x/y fields display and accept edge coordinates (labels « x (bord gauche) » / « y (bord avant) », conversion via displayValue/modelValue); global fields labelled with their axis (« Largeur (x) », « Profondeur (y) », corridors « (y) ») to defuse the width/depth ambiguity of the corner camera view
- [web/public/js/main.js] Updated: project section (apply/create/update/delete, project runs attached via projectId, comparison filtered by active project), warehouse selector + create/duplicate/delete (delete confirm warns about cascaded runs/projects), 3D edit mode orchestration (sim paused and disposed, working-definition validation keeping the last valid scene, save via PUT then re-run, cancel restore), render-loop guards for null sim, JSON helper that surfaces French API error messages (and omits Content-Type on body-less requests — Fastify rejects empty typed bodies); wires the floating windows and tabs, keeps the mini play/clock of the collapsed title bar in sync, feeds the collapsed KPI summary and toggles the edit dot; edit-mode scene rebuilds (each change, save, cancel) pass `recenter: false` so the camera orientation chosen by the user survives; exposes a read-only `window.simstepsDebug` handle (camera, controls) for UI tests; the working definition is normalized on entering edit mode (zones as lists, explicit sizes) and shipping/receiving zones can be added, removed (≥ 1 of each) and auto-selected like aisles/workshops
- [web/public/index.html] Added Projet and Entrepôt panel sections (selectors, action buttons, hidden edit panel with selection/global properties and error list) and supporting CSS; hint element gets an id for edit-mode text swap; UX overhaul: the single sidebar is split into two floating windows (`#winMain` with pinned Lecture + Piloter/Configurer tabs, `#winKpi` with live KPIs + comparison), each with a draggable title bar, a collapse chevron and a live collapsed summary (mini play/clock left, KPI summary right); amber dot on the Configurer tab during 3D editing; hint moved to bottom-left; the edit panel becomes a third floating window (`#editPanel`, bottom-centre, draggable/collapsible) with properties laid out horizontally (Sélection · Propriétés globales · Terminer) instead of a sub-panel of the Configurer tab; `.window[hidden]` rule so the hidden attribute wins over the flex display; « + Expédition » and « + Réception » buttons added to the edit window
- [web/public/js/panels.js] Created: pure floating-window logic (position clamping, drag geometry, serialized position+collapse state tolerant to invalid storage, collapsed-title KPI summary in French)
- [web/public/js/windows.js] Created: DOM wiring of floating windows (title-bar drag via pointer capture, collapse chevron with live title bar, bring-to-front, re-clamp on resize, localStorage persistence, `reclamp` handle for windows shown after load, CSS right/bottom anchors cleared on explicit positioning) and Piloter/Configurer tabs with remembered active tab
- [tests/unit/web/panels.test.js] Created: clamping, drag geometry, state round-trip and fallback, KPI summary formatting
- [tests/ui/fenetres.spec.js] Created: collapse with live title bars (mini play drives playback, mini clock advances), drag by title bar, position and collapse restored after reload
- [tests/unit/server/projects.test.js] Created: project routes and run attachment via inject() with a parameterizable fake pool (SQL-fragment dispatch + call journal)
- [tests/unit/server/validate.test.js] Added validateProjectPayload cases
- [tests/unit/web/projects.test.js] Created: splitSettings/buildSettings/mergeProjectParams (slider > extras > scenario precedence)
- [tests/unit/web/editor-model.test.js] Created: editor model tests (snap, clamps, non-mutation, add/remove invariants, id propagation, bays=1 rejection, validation cases, duplicate deep-clone, minimal definition validity)
- [README.md] Updated: Projet and Entrepôt (3D editor) UI sections, project endpoints and projectId in the API table, projects table and runs.project_id in the schema table; `npm run test:ui` documented (Playwright against the Docker stack, disposable test data); UI section now describes the two draggable/collapsible floating windows and their tabs; warehouse JSON format documents shipping/receiving lists, aisle corridor `width` and zone `width`/`depth`, and the nearest-shipping B2B drop rule
- [playwright.config.js] Created: Playwright config — testDir tests/ui, single worker (shared database), software-WebGL launch arg, webServer `docker compose up` with reuseExistingServer
- [tests/ui/helpers.js] Created: shared UI-test helpers (unique names, app load with console-error collector, scene click-scan selection avoiding the bottom edit window, disposable warehouse factory ignoring leftover test copies, name-based API cleanup, Piloter/Configurer tab openers)
- [tests/ui/interface.spec.js] Created: smoke test (both windows and their tabs, running playback clock, populated selectors, clean console)
- [tests/ui/projets.spec.js] Created: full project lifecycle (create from current settings, attached run, project-filtered comparison, update, reapply, delete) + no-active-project message; navigates the Piloter/Configurer tabs
- [tests/ui/entrepots.spec.js] Created: warehouse duplicate / switch / delete cycle on a disposable warehouse (via the Configurer tab)
- [tests/ui/editeur.spec.js] Created: edit mode (paused sim, locked panel, amber dot on the Configurer tab), click-selection and constrained drag (length conserved, metre snap), bays=1 rejection, add aisle, save persisted, cancel not persisted, camera orientation preserved across edits and edit exit (with OrbitControls-damping settling), add + resize a shipping zone through the fields with list-format persistence; aisle-count expectations computed from the disposable warehouse instead of hardcoded; the drag test asserts corridor containment and edge-aligned-or-clamped snapping
- [package.json] Added @playwright/test devDependency and `test:ui` script
- [.gitignore] Added Playwright artifacts (test-results/, playwright-report/)
- [server/index.js] Added startup banner after listen: SimSteps in figlet-standard ASCII art with the version read dynamically from package.json, then server URL and environment, all through the Fastify logger; logger uses the pino-pretty transport outside production (readable multiline banner in dev) and structured JSON in production (pino-pretty is a devDependency, absent from the Docker image)
- [package.json] Added pino-pretty devDependency
- [server/app.js] Default rate limit raised from 300 to 1000 req/min/IP: the limit also covers static assets and a page load is ~30 requests, so 300 allowed only ~10 page loads per minute (the UI test suite legitimately tripped it — 42 × 429 observed)

## Notes
Full suite passes (67/67), no database needed for tests. End-to-end verified: `docker compose up` → migrations + seed → CRUD, run execution (KPIs identical to the CLI run, determinism confirmed), persistence across restart, idempotent seed, dev mode against the db container. Everything verified in a real browser: operators animate through aisles with state colors, live KPIs update during playback, spaghetti and heatmap toggle correctly, sliders re-run the simulation instantly, a run was saved to the DB from the UI, and the comparison table works across current settings, scenarios and saved runs (clean console). 93 tests pass. Helm chart validated with helm v3.16.4: lint OK (single INFO about missing icon), template OK in default mode (sub-chart StatefulSet rendered, DATABASE_URL pointing at its service), external-database + ingress mode, and existing-secret mode; explicit error when no database mode is set; migrate-cli verified against the running Docker database. Final end-to-end check from a clean state (docker compose down -v, up --build): migrations + seed on first boot, health OK, deterministic run via API, all frontend assets served, UI verified in a real browser with clean console; final image ~200 MB. Delivery plan complete.

Projects + 3D editor verification: 137 unit tests pass without a database. Fresh-volume check on a throwaway PostgreSQL: both migrations apply and the seed creates the example project with valid FKs, idempotently. Real-database curl cycle: project CRUD (400 on invalid settings in French, 404 on missing referenced warehouse, `name` rejected inside settings), run attached to a project, projectId filter, run surviving project deletion with project_id set to NULL. Real-browser session (clean console): project created from current settings and auto-selected, saved run attached to it, comparison limited to project runs; edit mode pauses the simulation, click-selects an aisle, drags it with metre snapping and corridor clamping, rejects bays=1 with a French error and a disabled save button, adds an aisle, persists via PUT and resumes the simulation on the new plan; cancel discards changes; warehouse duplicate/switch/delete cycle works (a body-less DELETE bug caused by a stray Content-Type header was found and fixed this way). Test artifacts cleaned up afterwards (example warehouse restored, test project and run deleted).

UI test suite: 10 Playwright tests pass (~1 min) against the running Docker stack; unit suite green (146) and the database is left clean afterwards (only the example warehouse remains).

Multiple shipping/receiving zones and resizable elements: a warehouse can now hold several shipping and several receiving zones (added/removed from the edit window like aisles and workshops, at least one of each preserved; B2B drops go to the nearest shipping zone) and every element is resizable through the property fields (aisle corridor width, zone width/depth). The JSON format stays backward compatible: single-object shipping/receiving and implicit sizes are still accepted everywhere; the editor normalizes to lists with explicit sizes on save. Verified: 158 unit tests (engine nearest-shipping drop, object-or-list building, geometry, editor operations, server validation) and 12 UI tests pass; visual check in Chrome on a disposable copy showed the widened aisle corridor and two shipping zones of different sizes rendered correctly. During this work the example warehouse in the dev database was found polluted (7 aisles, moved shipping zone — traces of an interactive editing session left open) and was restored from data/warehouse-example.json; test helpers and expectations were hardened against such drift (leftover test copies ignored, aisle counts computed instead of hardcoded).

UX rework (two floating windows): verified in a real browser via chrome-devtools on the rebuilt Docker image — Piloter tab by default, Configurer tab with project/warehouse CRUD, Indicateurs window collapsing to a live summary ("11,8 cmd/h · 23,7 %"), 3D playback intact; a CSS specificity bug (the tab dot's explicit display overriding the hidden attribute) was caught by the UI suite and fixed with a `[hidden]` rule.

Release v0.3.0 published (floating-windows UX rework, dedicated horizontal edit window, camera kept during editing, multiple shipping/receiving zones with nearest-shipping B2B drops, resizable elements, edge-aligned grid snapping with integer panel coordinates): full suite green beforehand (162 unit + 12 UI), tag pushed, Docker Hub multi-arch image published and chart auto-aligned by the workflow.

Release v0.2.0 published (projects, 3D warehouse editor, Playwright UI tests, startup banner, dev-friendly logs, 1000 req/min rate limit): full test suite green beforehand (137 unit + 8 UI), tag pushed, Docker Hub multi-arch image published and chart auto-aligned by the workflow.

Repository lifecycle: git repository initialized and pushed to github.com/PPCM/simsteps (local history rebased onto the pre-existing Apache-2.0 initial commit); git global identity fixed (name/email were swapped). CodeQL enabled: first analysis raised 15 js/missing-rate-limiting alerts, fixed by the global rate limit; the alerts remained open because CodeQL only models the old package name `fastify-rate-limit` (MissingRateLimiting.qll:192), so they were dismissed as false positives with an explanatory comment. Releases v0.1.0 (Node 22), v0.1.1 (Node 24) and v0.1.2 published to Docker Hub (ppcm/simsteps, amd64+arm64); v0.1.2 validated the automatic chart alignment (bot commit on main). Release cycle: `npm version patch|minor|major` + `git push && git push --tags`, then `git pull`.
